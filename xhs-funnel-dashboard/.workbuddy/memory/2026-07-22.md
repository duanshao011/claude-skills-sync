# 2026-07-22

- 完成项目全架构只读审查：确认其为 Python 离线 ETL/指标计算 + 原生 JS/ECharts 单 HTML 看板。
- 已生成 `overview.md`，记录模块职责、数据流、前端状态结构和风险优先级。
- 当前关键风险：模板缺失查询面板 DOM 导致筛选/CSV 导出不可用；主表仅取星河∪薯条违背四表任意组合契约；多份薯条可能重复累计；新薯条仅 real_pay 时必需字段校验可能失败；成本时序为近似自然日对齐。
- Python 5 个模块和 `assets/dashboard.js` 语法检查均通过；本次未修改业务代码。
- 修复图表四全链路表格的表头穿模：移除分组表头 hover 时将背景设为 `inherit` 的规则，避免 sticky 表头悬停后变透明并露出下方数据；仅修改 `assets/dashboard.css`，CSS 回归断言、diff 格式和 JS 语法检查通过。
- 用户重新打开最新成品后确认表头穿模已解决；此前旧单 HTML 内联的是旧 CSS，需重新生成/打开成品才生效。
- 按用户确认锁定薯条唯一金额口径：投放金额只取“实际支付金额”且仅订单状态“推广完成”；已删除“实际消耗”映射和回退，缺失 real_pay 时明确加载失败。数据级测试验证忽略实际消耗、过滤非完成订单和缺列报错，Python/JS/格式检查通过。
- 第5项“成本时序近似”已解释：薯条启动/实付日期与星河归因行为日期不是同一业务时点，逐日成本可能错配；周期累计指标更可靠，严格逐日效率需订单级归因链路。
- 用户确认整体 ROI/累计指标保持不变，继续作为核心判断。
- 完成第一轮其余问题优化：模板接回图表四查询/重置/CSV导出7个控件；主表改为四表 note_id 并集并通过15种非空组合测试；多份薯条改为合并明细后按订单ID去重（后传优先）再按笔记汇总。
- 真实3份薯条验证：9437行去重48行，9389个有效订单、454篇笔记、实际支付总额1127785元；生成 `优化验证_全链路投放看板.html`，整体ROI 12.37，既有公式未变。
- 建立全链路爆款笔记评分：仅纳入四表齐全且进店/加购/成交均大于0的367篇；各指标按候选集百分位标准化，权重为内容15%、进店20%、加购20%、成交25%、商业效率10%、人群资产10%。TOP5依次为唯爱酱香饼、知名素人小赵、叁点后、一只数码猫、Boring的日记；三套权重敏感性测试下名单完全稳定。
