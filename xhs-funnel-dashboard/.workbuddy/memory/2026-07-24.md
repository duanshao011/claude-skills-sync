# 2026-07-24

- 单篇成本分析柱状图 tooltip 调整为仅显示“当日消耗 / 当日阅读成本 / 当日进店成本”，删除“累计进店成本”行；当日阅读成本口径为当日实付 ÷ 当日星河阅读/播放UV，保留图上的累计成本曲线。
- 首次覆盖生成时，默认扫描误把新增的星河分日趋势宽表当作标准星河明细，导致星河加载失败；已立即用覆盖前备份恢复原数据，并将 tooltip 改动直接注入原 HTML。最终校验星河已加载、GMV 14049808.26、整体ROI 12.26，原数据无损。
- 成本分析全部笔记汇总模式 tooltip 增加为5项：当日总消耗、当日投放笔记数、当日阅读成本、当日进店成本、平均每篇消耗；两项日成本分别按当日实付÷当日星河阅读UV、当日实付÷当日进店UV计算，0分母显示“—”。原HTML已备份后直接注入更新，未重跑数据链路。
- 已将7月22-24日全部确认改动统一固化到现有 `SKILL.md`：四图正确编号、实际支付唯一口径、订单ID去重、四表并集、查询导出、爆款评分、图表三6卡与双模式tooltip、表头穿模规则、原HTML备份覆盖、星河派生宽表避坑和成品硬校验；关键规则与格式检查通过。
- 核对图表三成本折线现行逻辑：阅读/进店/加购/成交均为逐日累计成本，公式是截至当天累计薯条实付 ÷ 截至当天累计星河对应UV；薯条启动日与星河归因日期按自然日对齐，是近似口径，日期不重合时仍纳入各自累计。
- 用户确认UI采用“小红书商业化专业版”：雾灰底、白色内容画布、品牌红重点、浅粉状态、轻边框/轻阴影/克制圆角。已仅通过 `dashboard.css` 视觉覆盖层重塑目录、页头、KPI、数据源、四图、筛选、表格、弹窗和响应式；DOM、JavaScript、payload、指标和交互未改。原HTML已备份为 `全链路投放看板_UI更新前备份_20260724-160309.html` 后只替换内联CSS；校验星河、GMV 14049808.26、整体ROI 12.26和关键DOM正常，并同步写入 `SKILL.md`。
- 修复图表下方指标胶囊“选中后悬浮变白、白字不可见”：限制白底 hover 仅作用于未选中项，`.active:hover` 保持指标色背景和白字。图表四下拉改为固定三列网格并隐藏横向溢出；同时发现历史标准化蒲公英文件被 `header=2` 错读，新增 `header=0` 自动回退，修复笔记ID/达人昵称/发布日期整体错位。明确排除两份星河派生宽表后重新生成原 HTML，数据恢复为1176篇，星河已加载、GMV 14049808.26、ROI 12.26。
- 用户确认图表三阅读成本统一按淘宝星河UV计算。已将单篇顶部“阅读成本”切换卡从蒲公英阅读UV改为 `s.uv_cost`（薯条累计实付÷星河阅读/播放UV），与指标卡、累计折线、单日tooltip口径一致；更新前备份原HTML并直接注入，校验星河已加载、GMV和ROI仍有效。
- 从业务判断明确成本趋势的推荐展示：主折线采用3日滚动成本（近3日实付合计÷近3日星河对应UV），累计成本保留为细灰虚线基线，单日成本仅放tooltip；原因是单日噪声大，累计口径反应迟钝，滚动口径更适合识别近期变贵或变便宜。
- 按用户要求删除图表四二级筛选整行（达人昵称、笔记ID、发布日期、查询、重置、导出CSV），保留右上角联动搜索、自定义列、排序和分页。原HTML备份后仅删除对应DOM，payload与JavaScript哈希不变，GMV 14049808.26、ROI 12.26。
- 用户进一步确认：图表三指标胶囊数值继续保持周期累计口径，只改变折线为趋势口径。多指标同时选中时推荐自动使用上下分面小图：每个指标独立Y轴、共享日期与联动tooltip，避免阅读/进店/加购/成交成本数量级差异造成压线；单选时维持一张主图。
- 已完成图表三成本趋势升级并直接更新原HTML：胶囊/KPI累计口径不变；单选显示每日实付柱+3日滚动成本实线+累计成本灰虚线；多选自动上下分面、独立Y轴、共享日期/缩放/tooltip；tooltip逐项展示当日、3日滚动、累计成本。备份为 `全链路投放看板_覆盖前备份_20260724-165133.html`，校验星河、GMV 14049808.26、ROI 12.26及内嵌JS语法正常。
- 已把截至本轮的全部最终生效规则全量迭代进投放看板 Skill，并同步项目级与用户级两份 `SKILL.md`：修正旧frontmatter，补齐输入模式、星河阅读UV统一、累计胶囊、3日滚动、累计基线、多指标分面、daily索引、业务解读、图表四最终交互、视觉规范、loader回退、派生宽表避坑、安全覆盖与硬校验；两份文件完全一致且格式检查通过。
